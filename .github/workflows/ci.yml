name: Run Tests in Docker

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write
  actions: read

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Pre-clean directories
        run: |
          sudo rm -rf ${{ github.workspace }}/test-results/* 2>/dev/null || true
          sudo rm -rf ${{ github.workspace }}/test-reports/* 2>/dev/null || true

      - name: Create output directories
        run: |
          mkdir -p ${{ github.workspace }}/test-results
          mkdir -p ${{ github.workspace }}/test-reports
          chmod 777 ${{ github.workspace }}/test-results
          chmod 777 ${{ github.workspace }}/test-reports

      - name: Build Docker image
        run: |
          docker build \
            --no-cache \
            -t playwright-allure-test \
            .

      - name: Run tests in container
        run: |
          docker run \
            --rm \
            --name test-runner \
            -v ${{ github.workspace }}/test-results:/app/allure-results \
            -v ${{ github.workspace }}/test-reports:/app/allure-report \
            -e SKIP_CLEANUP=true \
            --user $(id -u):$(id -g) \
            playwright-allure-test

      - name: List test results (debug)
        if: always()
        run: |
          echo "Contents of test-results:"
          ls -la ${{ github.workspace }}/test-results/ || echo "Directory is empty or doesn't exist"
          echo "Contents of test-reports:"
          ls -la ${{ github.workspace }}/test-reports/ || echo "Directory is empty or doesn't exist"

      - name: Upload Allure Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: test-results/
          retention-days: 30

      - name: Upload Allure Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-reports
          path: test-reports/
          retention-days: 30

      - name: Generate Allure Report
        if: always()
        uses: simple-elf/allure-report-action@master
        with:
          allure_results: test-results
          allure_report: allure-report-gh
          gh_pages: gh-pages
          allure_history: allure-history

      - name: Setup Pages
        if: always()
        uses: actions/configure-pages@v4

      - name: Upload to GitHub Pages
        if: always()
        uses: actions/upload-pages-artifact@v3
        with:
          path: allure-report-gh

      - name: Deploy to GitHub Pages
        if: always()
        id: deployment
        uses: actions/deploy-pages@v4